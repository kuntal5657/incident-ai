[project]
name = "incident-ai"
version = "0.1.0"
description = "Production-grade Incident Analysis using OpenAI (RAG + Fine-Tuning)"
requires-python = ">=3.10"

dependencies = [
    "openai>=1.12.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.5.0",
]

[tool.setuptools]
packages = ["app"]
